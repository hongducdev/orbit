<Page
    x:Class="Orbit.AnalyzePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Enabled"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="32">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12" MaxWidth="560">
            <FontIcon Glyph="&#xEDA3;" Style="{StaticResource OrbitPlaceholderIconStyle}" Foreground="{StaticResource OrbitJupiterBrush}" />
            <TextBlock Text="Jupiter — Analyze" Style="{StaticResource OrbitTitleTextStyle}" HorizontalAlignment="Center" FontSize="28" />
            <TextBlock Text="Treemap disk analyzer with drill-down, hardlink-aware sizing, and Recycle Bin actions."
                       TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center"
                       Style="{StaticResource OrbitBodySecondaryStyle}" />
            <Border Style="{StaticResource MoleCardStyle}" Margin="0,12,0,0">
                <StackPanel Spacing="8">
                    <TextBlock Text="Coming in Phase 4" FontWeight="SemiBold" />
                    <TextBlock Text="Squarified layout on Canvas, FindFirstFileExW scanning, reparse-point safe, progressive load for 1M+ files."
                               TextWrapping="Wrap" Opacity="0.7" />
                    <HyperlinkButton Content="Mole reference — mo analyze" NavigateUri="https://mole.fit" />
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Page>
